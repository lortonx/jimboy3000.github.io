var l=window.ogario;
l.selectBiggestCell ? l.playerMaxMass : l.playerMinMass;

l.biggerSTECellsCache.filter(function(cell){
            return cell.size;
        });
		
		
ctx.setTransform(1, 0, 0, 1, 0, 0);
		
